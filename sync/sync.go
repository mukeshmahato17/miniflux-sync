package sync

import (
	"log"
)

// Sync reads Subflux feeds and categories from a YAML file and syncs them with a SubFlux instance
// via the Subflux API.
func Sync() error {
	log.Println("syncing config")
	return nil
}
